export const siteConfig = {
  name: 'SceneForge AI',
  description: 'Generate compelling script scenes with the power of AI. Your vision, our technology.',
  // Shrinking Header settings
  enableShrinkingHeader: true,
  headerMaxHeight: 96, // Corresponds to h-24 in Tailwind
  headerMinHeight: 64, // Corresponds to h-16 in Tailwind
};

export type SiteConfig = typeof siteConfig;
